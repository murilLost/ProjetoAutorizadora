server:
  port: 8080

spring:
  datasource:
    url: jdbc:h2:~/MBA/projects/spring/h2/acquirerdb;DB_CLOSE_ON_EXIT=FALSE
    username: fiap
    password: fiap
  h2:
    console:
      enabled: true
      path: /h2-console
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true

  mail:
    host: smtp.gmail.com
    port: 587
    username: anac.cola@gmail.com
    password:
    protocol: smtp
    properties:
      mail:
        to:
      smtp:
        auth: true
        connection-timeout: 5000
        timeout: 5000
        starttls:
          enable: true